# 要跟踪的事件名
#ep.track: sys.starting,sys.started
server.hibernate:
  hibernate.hbm2ddl.auto: update
  ds:
    url: jdbc:h2:e:/tmp/h2/my-tpl;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: root
    password: root
    # testWhileIdle: false
    testWhileIdle: true
    validationQuery: select 1
    minIdle: 2
    maxActive: 5